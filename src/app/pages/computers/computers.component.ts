import { Component } from '@angular/core';

@Component({
    selector: 'computers-cmp',
    moduleId: module.id,
    templateUrl: 'computers.component.html'
})

export class ComputersComponent {
  postGroup: any;
}
